[pytest]
# Modules live under src/ziplex/ (a real installable package -- see
# pyproject.toml) and are imported in tests as `from ziplex.file.collector
# import ...`. Adding src/ to pythonpath here makes `ziplex` importable
# without an editable install being a hard requirement for running tests.
pythonpath = src
testpaths = tests

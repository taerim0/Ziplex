# Screenshots referenced by the main README

Drop screenshots here with these exact filenames and both `README.md`/`README_ko.md`'s GUI section picks them up automatically — no other change needed.

| Filename | Screen |
|---|---|
| `gui-pack-select.png` | Pack: project folder picked, files checked off for inclusion |
| `gui-pack-review.png` | Pack: post-analysis review (project guide/rules/per-file summaries) |
| `gui-relationships.png` | Dependency tree overview or the per-file relationship editor |
| `gui-overview.png` | An already-packed project's Overview page |
| `gui-options.png` | The Options page |

PNG preferred (screenshots compress losslessly and stay sharp on text-heavy UI); keep each under ~500KB so the README stays quick to load. No fixed resolution requirement — a plain window screenshot at native size is fine.
